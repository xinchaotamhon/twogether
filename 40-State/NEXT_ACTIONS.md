---
last_verified: 2026-09-04
verified_by: Codex session-study and completed-English integration
status: active
---

# Next Actions

1. **Deploy and learn now** — use the full-screen tree and centered session flip cards. `Quên` is only a temporary same-tab list; refresh preserves it, closing the tab clears it.
2. **Review only seven vocabulary cards** — approve suitable `Vocabulary Focus` items in `Thẻ`; the 74 other Empower cards are already connected to the English tree under Hiệp's explicit scope decision.
3. **Capture real learning failures** — create versioned edits for unclear prompts, incorrect explanations or transfer mismatches and update `docs/CONTENT_STYLE_HIEP_HOANG.md` when a reusable pattern appears.
4. **Restore durable progress only on request** — set `VITE_STUDY_MODE=fsrs`; then choose browser-local durability or first add the 74 approved Empower records (plus any newly approved vocabulary) to an additive Supabase content migration and complete pairing/RLS tests for cross-device history.
5. **Expand from observed need** — pronunciation/vocabulary/comparison remain planned English branches; React later becomes its own domain branch with authoritative sources and a new approval manifest.

Do not spend money, request notification permission, erase preserved history, re-enable cloud writes implicitly, or weaken learner isolation.
