---
last_verified: 2026-09-01
verified_by: Codex owner-approved v2 and inline-map study implementation
status: active
---

# Next Actions

1. **Activate and prove remote sync** — run `CONNECT_SUPABASE.bat`, enable Anonymous Sign-Ins, run the prepared SQL, then pair separate Hiệp/Hoàng browser profiles and execute real RLS/idempotency/conflict/streak tests. Host/key reachability alone is not sync.
2. **Human-review Empower A2** — flag cards needing correction and merge only accepted long-term cards. Preserve source pages and English Core prerequisites.
3. **Deploy and run a seven-day learning pilot** — choose a deck directly on the tree, complete it in the inline flashcard panel and record confusing prompts, weak transfer or excessive workload. Do not clear browser data during deploy.
4. **Expand from observed need** — add comparison, sound/word form and lexicon branches with variable card counts. Build React later as a separate domain branch from authoritative React sources and a new Human approval manifest.

Do not spend money, request notification permission, erase local history, or weaken learner isolation to accelerate any step.
